# CC2006
AI project (2025/26) for PopOut. Implements Monte Carlo Tree Search (UCT) and a custom ID3 Decision Tree algorithm. Features MCTS-driven data generation to train the tree for move prediction. Supports Human vs. CPU and CPU vs. CPU modes, built from scratch without external ML libraries to explore adversarial search and symbolic learning.
