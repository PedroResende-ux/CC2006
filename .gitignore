# Python bytecode
__pycache__/
*.pyc

# pytest cache
.pytest_cache/

# Numba JIT cache (regenerated on first import of ai.rollout_numba)
*.nbi
*.nbc

# Jupyter checkpoints
.ipynb_checkpoints/

# Large source datasets — too big for git; regenerable via scripts/generate_dataset.py
data/popout_200k.csv
data/popout_dataset_150k.csv

# Claude Code local config (not part of the submission)
.claude/
CLAUDE.md

# Debug log from an aborted sweep run
_audits/sweep_run.log
